name: Scheduled Job

on:
  schedule:
    - cron: '30 8 * * *'  # Runs daily at midnight

jobs:
  example:
    runs-on: ubuntu-latest
    steps:
      - name: Show actor
        run: echo "Triggered by ${{ github.actor }}"
